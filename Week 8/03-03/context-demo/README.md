### Context Demo in React Applications
1. Create Context Object

2. Every Context Object has a context provider and context consumer

3. Context Provider is used to provide state through context object whereas the components can consume the state through the context object.

4. A component can consume the state of context provider with useContext hook